import { EmailValidator } from '@angular/forms';

export interface usersdata {
  id: number;
  name: string;
  email: string;
  mobile: number;
  department: number;
  city : string;
}


export interface address {
  address: string;
}
